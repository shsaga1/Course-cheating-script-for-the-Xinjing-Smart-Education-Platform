# 开发日志 / Dev Log

## 项目目标

为课程页面编写一个自动化脚本，实现以下能力：

- **后台运行**：规避平台的反挂机检测
- **自动化切换**：视频/音频自动播放并在结束后切换到下一个对象
- **文本确认**：文档页自动点击“完成学习”类按钮
- 习题先跳过，不做自动答题

---

## 开发过程记录

### 初始现象：切到后台后视频停止

**问题现象**

- 页面前台播放正常
- 浏览器最小化或切到其他页面后，视频/学习进度不再正常推进

**最开始的思路**

- 使用UI心跳或者API心跳模拟用户活跃

**运行结果**

失败，后续把方向转为排查浏览器自身问题

**排查结果**

通过加浏览器参数

-disable-background-timer-throttling
-disable-renderer-backgrounding
-disable-backgrounding-occluded-windows
-autoplay-policy=no-user-gesture-required

后续执行脚本仍然失败。

进行各种暂停逻辑测试观察到：

- 页面暂停后仍可通过浏览器“媒体中心”重新播放
- 播放页面切入后台瞬间便会自动执行暂停机制

**结论**

脚本没有问题，页面会检测状态并执行暂停业务。

---

###  定位到真正的暂停业务

**问题**

需要确认后台停止播放到底是谁触发的。

**做法**

在业务代码中继续向下追踪，

最初通过关键词'pause'等进行搜索，定位到

aegisInject.3033925e.js

后续发现定位错误，转向检测执行程序

检测到切入后台时

h5_daxuesouti?...文件会开始活动，并依此定位到关键文件

（中途被ai误导转向研究
progress/?team_id=9620
文件）


最终追溯发现组件中存在明确逻辑：

- 页面触发 `visibilitychange`
- 当 `document.visibilityState !== "visible"` 时
- 调用 `this.videoPlayer.pause()`

**结论**

后台停止播放的直接业务链路为：

```text
visibilitychange -> visiblityChange() -> videoPlayer.pause()
```

这说明问题根因不是“自然暂停”，而是页面自己主动调用了 `pause()`。

---

### 开发第二版脚本

**第二版思路**

尝试用 Selenium 注入全局补丁：

- 改写 `document.visibilityState`
- 改写 `document.hidden`
- 捕获并拦截 `visibilitychange`
- 直接通过 `document.getElementById("videoPlayer")` 恢复播放

**遇到的问题**

- 状态检测返回 `None`
- 拿不到 `videoPlayer`

**原因分析**

这一版的问题在于：

- 先补丁，后验证
- 页面检测不到’videoPlayer’对象

**结论**

videoPlayer可能并不是真实媒体对象

---

### 动态对象验证：确认 `videoPlayer` 就是真实媒体对象

**问题**

必须确认页面里真正控制播放的对象到底是谁。

**做法**

改为从运行中的 Vue 实例入手，扫描带以下字段的组件：

- `videoPlayer`
- `currentVideoTime`
- `lastVideoTime`
- `timeStep`
- `isSending`
- `isVideoEnd`
- `currentVideoDuration`

最终成功定位到目标组件实例，并确认：

- `videoPlayer` 是真实 `HTMLVideoElement`
- `videoPlayer.id === "videoPlayer"`
- `videoPlayer.tagName === "VIDEO"`

**结论**

问题应该出自本地组件与浏览器扫到的对象不一样

---

### （ai要求动态检验pause()是否执行，被我阻止）

---

### 第三版代码：DevTools页面内扫描并挂载对象，本地运行脚本 

**问题**

Selenium 与 DevTools 上下文不一致，虽然浏览器内挂载成功但是Selenium识别不到。后续尝试让 Selenium 自动扫描 Vue 实例并挂载目标对象，但同样失败

**现象**

- DevTools Console 里能找到 `window.__targetVm` 和 `window.__targetVideo`
- Selenium `execute_script()` 中读不到对应对象
- 自动扫描结果 `hitsCount = 0`

**原因**

页面调试上下文与 Selenium 执行上下文不完全一致，导致：

- 控制台里可见的对象
- 不一定能被自动化脚本用同样方式直接读到

**解决方式**

转向纯前端脚本方案，把核心逻辑全部放到页面内执行。

**结论**
转向研究纯前端脚本。

(由于本人缺乏前端开发经验，从发现现象到得到这个结论前后经历了3h，让本人身心疲惫，因此直接放弃了使用本地自动化脚本而是完全转向浏览器内脚本)

---

### 第四版脚本：纯前端脚本

**做法**

开始直接解析左侧目录中的课程项。

初步识别出以下对象类型：

- 文档
- 音频
- 习题
- 视频

**遇到的问题**

第一版目录脚本虽然能跳过文档并进入音频，但“当前项识别”不稳定，所有表格中的 `current` 都是 `false`。

**原因**

目录中除了真正对象外，还混有：

- 模块标题
- 节标题
- 章节说明

而最初的识别逻辑过于宽泛。

**结论**

需要基于页面的真实 DOM 结构进一步收紧选择器。

---

### 9. 找到当前目录项的真实 DOM 特征

**排查方式**

手动在 DevTools 中选中当前播放项，分析 DOM 结构和样式。

**确认结果**

当前项特征如下：

- 外层对象容器：`div.courseware`
- 标题容器：`div.courseware__title`
- 当前项类名：`div.courseware__title.active`
- 标题文本节点：`span.title`
- 当前标题颜色：`rgb(0, 128, 255)`

**解决方式**

将目录解析逻辑改为：

- 只扫描 `div.courseware`
- 只在其内部找 `div.courseware__title > span.title`
- 当前项优先按 `.active` 判断
- 再用蓝色标题颜色兜底

**结论**

成功实现切换播放对象

---

### 10. 自动处理不同对象类型

**对象处理策略**

- 视频：自动播放、拦截暂停、结束后跳下一个
- 音频：自动播放、结束后跳下一个
- 文档：自动点击“完成学习/完成阅读/我已学习”等按钮，然后跳下一个
- 习题：暂时跳过

**实现**

- 直接点击目录里的下一个对象
- 加入防抖，避免接近结束时重复触发切换
- 对 `ended` 事件做重新绑定，避免旧监听残留

**结论**

自动切换逻辑跑通，核心业务需求基本实现。

---

### 文档页确认按钮处理

**问题**

文档页会出现一个需要人工点击的“完成学习”类按钮，不处理的话无法继续。

**定位结果**

按钮文字节点为：

- `span.fixed-action__label`

可能的文案包括：

- 点击完成学习
- 完成学习
- 完成阅读
- 我已学习

**解决方式**

脚本中加入：

- 自动查找 `span.fixed-action__label`
- 自动点击
- 检查完成状态
- 完成后切换到下一个对象

**结论**

文档页可以自动完成，不再需要手工确认。

---

## 当前成果

目前脚本已具备：

- 自动识别课程目录对象
- 自动识别当前项
- 后台拦截媒体暂停
- 视频/音频自动播放
- 文档自动完成
- 习题自动跳过
- 对象结束后自动切换到下一个对象
- 以 Tampermonkey 形式自动注入页面

---

## 后续可继续优化的方向

### 1. 习题处理
目前只跳过，后续如有需要可增加自动作答逻辑。

### 2. UI 控制面板
可以增加开关配置，例如：

- 是否跳过习题
- 是否自动完成文档
- 是否打印调试日志
- 是否自动静音

### 3. 更稳的完成状态识别
文档完成状态目前依赖按钮文本 / class / aria 属性，可继续按页面实际逻辑收紧。

### 4. 日志持久化
当前主要依赖控制台日志，后续可考虑持久化到 LocalStorage 方便复盘。

