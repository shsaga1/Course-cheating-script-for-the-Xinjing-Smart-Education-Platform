# 开发日志 / Dev Log

## 项目目标

为课程页面编写一个自动化脚本，实现以下能力：

- **后台运行**：规避平台的反挂机检测
- **自动化切换**：视频/音频自动播放并在结束后切换到下一个对象
- **文本确认**：文档页自动点击“完成学习”类按钮
- 习题先跳过，不做自动答题

---

## 开发过程记录

### 初始现象：切到后台后视频停止

**问题现象**

- 页面前台播放正常
- 浏览器最小化或切到其他页面后，视频/学习进度不再正常推进

**最开始的思路**

- 使用UI心跳或者API心跳模拟用户活跃

**运行结果**

失败，后续把方向转为排查浏览器自身问题

**排查结果**

通过加浏览器参数

-disable-background-timer-throttling
-disable-renderer-backgrounding
-disable-backgrounding-occluded-windows
-autoplay-policy=no-user-gesture-required

后续执行脚本仍然失败。

进行各种暂停逻辑测试观察到：

- 页面暂停后仍可通过浏览器“媒体中心”重新播放
- 播放页面切入后台瞬间便会自动执行暂停机制

**结论**

脚本没有问题，页面会检测状态并执行暂停业务。

---

###  定位到真正的暂停业务

**问题**

需要确认后台停止播放到底是谁触发的。

**做法**

在业务代码中继续向下追踪，最终发现组件中存在明确逻辑：

- 页面触发 `visibilitychange`
- 当 `document.visibilityState !== "visible"` 时
- 调用 `this.videoPlayer.pause()`

**结论**

后台停止播放的直接业务链路为：

```text
visibilitychange -> visiblityChange() -> videoPlayer.pause()
```

这说明问题根因不是“自然暂停”，而是页面自己主动调用了 `pause()`。

---

### 3. 第一版修复方案失败：页面级补丁不够稳

**第一版思路**

尝试用 Selenium 注入全局补丁：

- 改写 `document.visibilityState`
- 改写 `document.hidden`
- 捕获并拦截 `visibilitychange`
- 直接通过 `document.getElementById("videoPlayer")` 恢复播放

**遇到的问题**

- 状态检测经常返回 `None`
- 有时拿不到 `videoPlayer`
- 虽然脚本逻辑看起来合理，但效果不稳定

**原因分析**

这一版的问题不在于方向完全错误，而在于：

- 先补丁，后验证
- 从页面表层找对象，而不是从业务实例找对象
- 拦的是“事件入口”，不是“最终动作”

**结论**

需要进入“运行时实例级验证”，不能只停留在页面级补丁。

---

### 4. 动态对象验证：确认 `videoPlayer` 就是真实媒体对象

**问题**

必须确认页面里真正控制播放的对象到底是谁。

**做法**

改为从运行中的 Vue 实例入手，扫描带以下字段的组件：

- `videoPlayer`
- `currentVideoTime`
- `lastVideoTime`
- `timeStep`
- `isSending`
- `isVideoEnd`
- `currentVideoDuration`

最终成功定位到目标组件实例，并确认：

- `videoPlayer` 是真实 `HTMLVideoElement`
- `videoPlayer.id === "videoPlayer"`
- `videoPlayer.tagName === "VIDEO"`

**结论**

静态代码与运行时实例对上了。后续所有修复都应围绕真实实例对象展开，而不是继续盲目扫 DOM。

---

### 5. 运行时取证：确认后台确实调用了 `pause()`

**问题**

需要动态证明：后台切换时是否真的走了 `pause()` 这条链。

**做法**

在真实媒体对象上拦截 `pause()`，并记录日志。

**观察到的顺序**

```text
video_event_timeupdate
document_visibilitychange { hidden: true }
pause_blocked
video_event_timeupdate 继续发生
```

**结论**

这一步提供了关键运行时证据：

- 页面切后台时确实触发了 `visibilitychange`
- 随后确实调用了真实媒体对象的 `pause()`
- 当该 `pause()` 被拦住后，`timeupdate` 仍继续触发

因此可以确认：后台停止播放的直接原因就是业务组件中的主动暂停逻辑。

---

### 6. Selenium 与 DevTools 上下文不一致

**问题**

曾尝试让 Selenium 自动扫描 Vue 实例并挂载目标对象，但失败；而在浏览器控制台中手动执行相同逻辑时可以成功。

**现象**

- DevTools Console 里能找到 `window.__targetVm` 和 `window.__targetVideo`
- Selenium `execute_script()` 中却读不到对应对象
- 自动扫描结果 `hitsCount = 0`

**原因**

页面调试上下文与 Selenium 执行上下文不完全一致，导致：

- 控制台里可见的对象
- 不一定能被自动化脚本用同样方式直接读到

**解决方式**

放弃“先 DevTools 挂对象，再让 Selenium 读”的主路线，转向纯前端脚本方案，把核心逻辑全部放到页面内执行。

**结论**

对于当前项目，最稳的方案不是 Selenium 主导，而是浏览器内脚本主导。

(由于本人缺乏前端开发经验，从发现现象到让ai生成测试程序到得到这个结论前后经历了3h，让本人身心疲惫，因此直接放弃了使用本地自动化脚本而是完全转向浏览器内脚本)

---

### 7. 第二阶段需求：不仅要后台不断播，还要自动切换到下一个对象

**新增需求**

主管提出，仅仅保证当前视频后台不断播还不够，还需要：

- 自动处理下一个学习对象
- 页面里存在多种对象：视频、音频、文档、习题
- 遇到文档时自动完成
- 遇到习题先跳过
- 遇到音频/视频时自动进入并播放

**最初方案**

尝试点击“下一项”按钮。

**遇到的问题**

脚本虽然会发生跳转动作，但实际上还是停留在同一个视频对象内，没有真正切换到课程目录中的下一个对象。

**原因**

页面里的“下一项按钮”并不是可靠的课程对象导航源，真正的对象顺序在左侧课程目录中。

**结论**

切换逻辑必须改为“目录驱动”，而不是“播放器按钮驱动”。

---

### 8. 课程目录解析：从按钮驱动改为目录驱动

**做法**

开始直接解析左侧目录中的课程项。

初步识别出以下对象类型：

- 文档
- 音频
- 习题
- 视频

**遇到的问题**

第一版目录脚本虽然能跳过文档并进入音频，但“当前项识别”不稳定，所有表格中的 `current` 都是 `false`。

**原因**

目录中除了真正对象外，还混有：

- 模块标题
- 节标题
- 章节说明

而最初的识别逻辑过于宽泛。

**结论**

需要基于页面的真实 DOM 结构进一步收紧选择器。

---

### 9. 找到当前目录项的真实 DOM 特征

**排查方式**

手动在 DevTools 中选中当前播放项，分析 DOM 结构和样式。

**确认结果**

当前项特征如下：

- 外层对象容器：`div.courseware`
- 标题容器：`div.courseware__title`
- 当前项类名：`div.courseware__title.active`
- 标题文本节点：`span.title`
- 当前标题颜色：`rgb(0, 128, 255)`

**解决方式**

将目录解析逻辑改为：

- 只扫描 `div.courseware`
- 只在其内部找 `div.courseware__title > span.title`
- 当前项优先按 `.active` 判断
- 再用蓝色标题颜色兜底

**结论**

当前项定位终于稳定，后续点击真正落在“下一个目录对象”而不再重复点当前项。

---

### 10. 自动处理不同对象类型

**最终对象处理策略**

- 视频：自动播放、拦截暂停、结束后跳下一个
- 音频：自动播放、结束后跳下一个
- 文档：自动点击“完成学习/完成阅读/我已学习”等按钮，然后跳下一个
- 习题：暂时跳过

**实现要点**

- 不再依赖页面上的“下一项按钮”
- 直接点击目录里的下一个对象
- 加入防抖，避免接近结束时重复触发切换
- 对 `ended` 事件做重新绑定，避免旧监听残留

**结论**

对象级自动切换逻辑跑通，核心业务需求基本实现。

---

### 11. 文档页确认按钮处理

**问题**

文档页会出现一个需要人工点击的“完成学习”类按钮，不处理的话无法继续。

**定位结果**

按钮文字节点为：

- `span.fixed-action__label`

可能的文案包括：

- 点击完成学习
- 完成学习
- 完成阅读
- 我已学习

**解决方式**

脚本中加入：

- 自动查找 `span.fixed-action__label`
- 自动点击
- 检查完成状态
- 完成后切换到下一个对象

**结论**

文档页可以自动完成，不再需要手工确认。

---

### 12. 最终落地形式：Tampermonkey 用户脚本

**为什么选它**

对当前项目而言，Tampermonkey 是最适合的落地形式，因为：

- 逻辑本质上是页面内 DOM 自动化
- 需要进入页面后自动执行
- 需要持续运行轮询
- 比 Selenium 更稳
- 比浏览器插件更轻量，适合快速迭代

**结果**

最终将控制台调试通过的脚本整理为：

- `tampermonkey-course-auto.user.js`
- 配套 `README.md`

并计划上传到 GitHub 作为项目仓库。

---

## 本项目中踩过的主要坑

### 坑 1：误把“浏览器后台暂停”当作浏览器行为
**实际情况**：是页面业务代码主动调用 `pause()`。

---

### 坑 2：过早写全局补丁
**问题**：没有先验证运行时真实对象。  
**改进**：先找真实实例，再做定点修复。

---

### 坑 3：依赖“下一项按钮”
**问题**：按钮并不等于课程目录的下一个对象。  
**改进**：改为目录驱动。

---

### 坑 4：目录解析过宽
**问题**：把模块标题、节标题也当成课程对象。  
**改进**：收紧到 `div.courseware`。

---

### 坑 5：当前项识别不准
**问题**：无法稳定判断现在正在第几项。  
**改进**：使用 `.courseware__title.active` 和蓝色标题作为识别依据。

---

### 坑 6：Selenium 与控制台上下文不一致
**问题**：控制台能读到，自动化里读不到。  
**改进**：把核心逻辑全部迁回页面内脚本。

---

## 当前成果

目前脚本已具备：

- 自动识别课程目录对象
- 自动识别当前项
- 后台拦截媒体暂停
- 视频/音频自动播放
- 文档自动完成
- 习题自动跳过
- 对象结束后自动切换到下一个对象
- 以 Tampermonkey 形式自动注入页面

---

## 后续可继续优化的方向

### 1. 习题处理
目前只跳过，后续如有需要可增加自动作答逻辑。

### 2. UI 控制面板
可以增加开关配置，例如：

- 是否跳过习题
- 是否自动完成文档
- 是否打印调试日志
- 是否自动静音

### 3. 更稳的完成状态识别
文档完成状态目前依赖按钮文本 / class / aria 属性，可继续按页面实际逻辑收紧。

### 4. 日志持久化
当前主要依赖控制台日志，后续可考虑持久化到 LocalStorage 方便复盘。

---

## 一句话总结

这个项目最终不是“写一个防后台暂停的小补丁”，而是逐步演化成了一个：

**面向课程目录对象的页面内自动导航与播放脚本。**
